var pasta = "Spaghetti"; // ES5 syntax

const meat = "Pancetta"; // ES6 syntax

let sauce = "Eggs and cheese"; // ES6 syntax

// Template literals, like the one below, were introduced in ES6
const carbonara = `You can make carbonara with ${pasta}, ${meat}, and a sauce made with ${sauce}.`;

//just showed the errors that could come up with browser compatabilities


// There is always a gap in syntax adoption by developers and browsers. Developers need to be aware of the potential syntax errors that can occur in older browsers so they may need to check or make their code backward compatible
