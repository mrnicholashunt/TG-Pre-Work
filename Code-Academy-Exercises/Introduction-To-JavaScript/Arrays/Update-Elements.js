let groceryList = ['bread', 'tomatoes', 'milk'];

groceryList[1] = 'avocados';


// Once an element is accesible in a variable, it is easily reassigned but calling the variabe[number of element in array] = 'new value'
