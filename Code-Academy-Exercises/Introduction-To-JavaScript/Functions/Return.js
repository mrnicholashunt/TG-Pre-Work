function monitorCount(rows, columns){
  return(rows * columns)
}
const numOfMonitors = monitorCount(5, 4);
console.log (numOfMonitors)


//The keyword return is used to capture the results of a function that has been called upon
