let tool = 'marker';

// Use short circuit evaluation to assign  writingUtensil variable below:
let writingUtensil = tool || 'pen';

console.log(`The ${writingUtensil} is mightier than the sword.`);

// This functionality allows for a developer to check to see if a variable value assignment exists, if it does, it will use that value, if not, it can then assign a value.
