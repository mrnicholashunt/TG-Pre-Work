console.log(Math.floor(Math.random() * 100))
console.log(Math.ceil(43.8))
console.log(Number.isInteger(2017))
//js has many built in objects which can be called upon for various uses. Each object also has methods to expand functionality
